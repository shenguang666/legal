## 1. 配置与数据模型

- [x] 1.1 扩展 Elasticsearch 配置，新增 MinerU 知识库索引名称和智能问答默认知识库检索范围，并为新增 YAML 配置添加中文注释。
- [x] 1.2 新增智能问答知识库检索范围枚举，枚举类和枚举值添加中文注释。
- [x] 1.3 新增数据库配置结构或配置项，用于持久化智能问答知识库检索范围，字段和实体属性添加中文注释。
- [x] 1.4 新增增量 SQL，创建或更新保存智能问答检索范围所需的数据库对象。

## 2. 索引写入与删除路由

- [x] 2.1 改造知识库索引服务，使知识库文档按解析方式写入原生索引或 MinerU 索引。
- [x] 2.2 确保风险规则索引和天眼审查链路不受知识库解析方式分索引影响。
- [x] 2.3 改造文档删除和知识库重置逻辑，同时清理原生知识库索引和 MinerU 知识库索引中的文档数据。

## 3. 智能问答索引范围控制

- [x] 3.1 新增配置服务，读取和保存智能问答知识库检索索引范围，未配置时回退到默认原生索引。
- [x] 3.2 改造 Elasticsearch 检索服务，支持按指定索引列表执行 RRF 混合检索并合并结果。
- [x] 3.3 改造智能问答 RAG 检索器，使其按管理配置选择原生索引、MinerU 索引或双索引。
- [x] 3.4 改造检索预览接口，使管理员预览结果与当前智能问答索引范围一致。

## 4. 管理页

- [x] 4.1 新增后端管理接口，返回可选索引范围、当前索引范围和实际索引名称。
- [x] 4.2 改造知识库管理页，新增智能问答知识库检索范围选择和保存按钮。
- [x] 4.3 保存成功后刷新配置展示，并给出明确的成功或失败提示。

## 5. 验证

- [x] 5.1 运行 `mvn -DskipTests compile` 验证后端编译。
- [x] 5.2 运行 `npm run build` 验证前端构建。
- [x] 5.3 检查新增 SQL、YAML、实体字段和枚举注释符合项目规则。
