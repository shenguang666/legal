## 1. 数据模型与状态机调整

- [x] 1.1 为 `user_memory_item` 增加候选态、连续命中计数、稳定升级阈值所需字段
- [x] 1.2 为 `user_knowledge` 增加 `knowledge_level`、`reason`、`core_content`、`reviewed_at` 和更明确的状态语义
- [x] 1.3 更新 `schema.sql`、实体类注释、Mapper 与相关枚举，保证新字段和状态机可用

## 2. 长期记忆稳定化实现

- [x] 2.1 重构长期记忆抽取服务，使其只基于摘要执行而不再逐轮识别
- [x] 2.2 实现长期记忆规范化键归一规则，统一 `job/职业/profession` 等同义键
- [x] 2.3 实现候选态、连续命中确认、稳定升级和冲突值保护逻辑
- [x] 2.4 调整上下文组装逻辑，仅把稳定长期记忆注入系统提示词

## 3. 外挂知识库治理实现

- [x] 3.1 实现用户问答的规则预过滤，剔除寒暄、临时指令、明显低价值内容
- [x] 3.2 实现 `must/optional/forbidden` 三级分类，并存储 `reason` 与 `core_content`
- [x] 3.3 实现 `must` 立即入库并写入 `legal_kb_user_knowledge` 索引的流程
- [x] 3.4 实现 `optional` 候选延迟复核流程，并在摘要阶段决定激活或拒绝
- [x] 3.5 实现基于 `core_content` 的去重与重复索引保护逻辑

## 4. 异步编排与验证

- [x] 4.1 调整 `memory_task_outbox` 工作器顺序：先摘要，再长期记忆抽取，再复核待定外挂知识
- [x] 4.2 更新 askStream/主对话链路的任务入队逻辑，确保长期记忆不逐轮触发
- [x] 4.3 编写或补充验证用例，覆盖候选升级、键归一化、`optional` 延迟复核与去重场景
- [x] 4.4 执行编译/测试并验证 OpenSpec 变更与实现保持一致
