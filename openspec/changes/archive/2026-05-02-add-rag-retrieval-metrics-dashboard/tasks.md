## 1. 数据模型与配置

- [x] 1.1 使用 dbhub 或数据库检查确认实际 `retrieval_log` 表名、主键、`query_text`、租户字段、时间字段和命中文档字段格式。
- [x] 1.2 增加检索指标评估结果的表结构变更，包括检索日志引用、租户 ID、查询文本、原始命中、漏召回相关文档、TP、FN、FP、Recall、Precision、评估 TopN、模型元数据、状态、错误信息和时间字段。
- [x] 1.3 新增检索指标评估结果的 MyBatis Entity 和 Mapper。
- [x] 1.4 在专用 RAG 指标命名空间下新增 YAML 配置，并为开关、Cron、候选 TopN 默认 200、批次大小、每日最大样本数、并发、超时和模型选项添加说明注释。

## 2. 离线候选召回

- [x] 2.1 新增可复用检索服务方法，用于在不生成答案的情况下为 query 召回 TopN 候选文档。
- [x] 2.2 确保离线候选召回遵守租户范围和知识可见性规则。
- [x] 2.3 规范候选标识，使 TopN 候选能够与 `retrieval_log` 中的原始命中文档比较。
- [x] 2.4 增加候选召回数量限制、租户范围和标识比较的单元测试。

## 3. LLM 指标评估

- [x] 3.1 实现 LLM 提示词和解析器，用于判断原始命中文档是否与 `query_text` 相关。
- [x] 3.2 实现 LLM 提示词和解析器，用于判断原始命中之外的 TopN 候选文档是否为漏召回相关文档。
- [x] 3.3 使用 `Recall = TP / (TP + FN)` 和 `Precision = TP / (TP + FP)` 实现指标计算，并处理分母为 0 的情况。
- [x] 3.4 持久化模型解释、原始判断摘要、提示词/模型版本和评估状态，保证可审计。
- [x] 3.5 增加 TP/FN/FP 计数、分母为 0、解析失败和部分模型失败的测试。

## 4. 定时异步任务

- [x] 4.1 实现受 YAML 开关和 Cron 配置控制的定时检索指标评估任务。
- [x] 4.2 按日期、状态、租户和幂等键加载符合条件的 `retrieval_log` 记录。
- [x] 4.3 按可配置批次处理日志，并预留并发和超时限制。
- [x] 4.4 将每条评估标记为 SUCCESS 或 FAILED，单条失败不阻断整个批次。
- [x] 4.5 增加任务禁用、批处理、幂等和失败项隔离测试。

## 5. 统计 API

- [x] 5.1 新增按日期范围查询聚合指标的 API，包括平均 Recall、平均 Precision、评估样本数、失败数和趋势数据。
- [x] 5.2 新增按日期范围查询评估明细的 API，包括 query 文本、原始命中、漏召回相关文档、TP/FN/FP、得分、状态和模型解释。
- [x] 5.3 增加日期范围、分页以及可选租户或来源筛选的请求校验。
- [x] 5.4 增加聚合计算、明细分页、失败样本计数和空日期范围的服务测试。

## 6. 前端看板

- [x] 6.1 新增 RAG 检索指标统计页面，支持日期范围筛选。
- [x] 6.2 展示平均 Recall、平均 Precision、评估样本数、失败样本数和当前评估 TopN。
- [x] 6.3 增加趋势可视化和按 query 展示的评估明细表。
- [x] 6.4 增加明细展开或弹窗，展示原始命中文档、漏召回相关文档和 LLM 解释。
- [x] 6.5 增加前端 API 集成测试或组件级检查，覆盖加载、空数据、错误和筛选状态。

## 7. 验证与发布

- [x] 7.1 运行后端单元测试和指标评估定向集成测试。
- [x] 7.2 运行前端构建或相关页面测试。
- [x] 7.3 使用测试数据小日期范围验证任务，并确认持久化 Recall/Precision 值。
- [x] 7.4 记录评估口径、YAML 设置、成本控制和回滚步骤。
