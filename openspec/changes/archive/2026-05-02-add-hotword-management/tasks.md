## 1. 数据库与模型

- [x] 1.1 在 `src/main/resources/db/schema.sql` 新增 `chat_hotword` 表，并为每个字段添加中文注释。
- [x] 1.2 新增热词实体类，实体类每个属性添加中文注释。
- [x] 1.3 新增热词 Mapper，并按当前项目 MyBatis Plus 结构接入。
- [x] 1.4 如新增枚举类或枚举值，为枚举类和枚举值添加中文注释。

## 2. 后端热词服务

- [x] 2.1 新增热词请求 DTO 和响应 VO，覆盖创建、编辑、状态切换、分页查询、随机读取场景。
- [x] 2.2 实现热词管理 Service，支持新增、编辑、启停、逻辑删除和分页查询。
- [x] 2.3 实现 Redis + MySQL 双级读取服务，优先读取 Redis 启用热词池，缺失时回源 MySQL 并回填缓存。
- [x] 2.4 在热词新增、编辑、启停、删除成功后失效当前租户 Redis 热词池缓存。
- [x] 2.5 实现随机读取逻辑，每次最多返回 3 条启用热词，不足 3 条时返回全部。
- [x] 2.6 新增热词 Controller，提供管理接口和聊天页随机读取接口。

## 3. 前端热词管理

- [x] 3.1 新增 `HotwordView.vue`，实现热词列表、分页刷新、新增、编辑、启停、删除确认等交互。
- [x] 3.2 在前端路由中注册“热词管理”页面。
- [x] 3.3 在主导航中新增“热词管理”入口，并保持当前 UI 风格一致。
- [x] 3.4 为热词表单补齐 `label`、`name`、按钮 `type`、错误提示和 `aria-live` 状态反馈。

## 4. 聊天页改造

- [x] 4.1 移除 `ChatView.vue` 中硬编码 `quickPrompts` 数组，改为调用随机热词接口。
- [x] 4.2 聊天页首次进入时加载最多 3 条热词。
- [x] 4.3 快捷问题区域新增“换一组”刷新按钮，点击后重新调用随机热词接口。
- [x] 4.4 随机热词接口失败或返回空列表时，聊天页展示友好空态且不影响手动输入。

## 5. 验证

- [x] 5.1 运行后端编译或相关测试，确认新增热词后端代码可编译。
- [x] 5.2 运行前端构建，确认热词管理页面和聊天页改造可编译。
- [x] 5.3 手动验证热词新增、编辑、启停、删除后聊天页刷新结果符合预期。
- [x] 5.4 验证 Redis 缓存命中、缓存缺失回源 MySQL、管理变更后缓存失效三个场景。

## 6. 热词预设答案直答

- [x] 6.1 扩展热词表、实体和 DTO，支持 `hotword_key` 与 `preset_answer`。
- [x] 6.2 改造热词随机接口，返回热词标记但不暴露预设答案。
- [x] 6.3 改造热词管理页面，支持维护热词标记和预设答案。
- [x] 6.4 改造聊天页，点击热词后发送 `askStream` 时携带热词标记，手动编辑输入框时清空标记。
- [x] 6.5 改造后端 `askStream`，命中有效热词标记时直接返回预设答案并跳过 RAG 与大模型链路。
- [x] 6.6 验证 live MySQL 迁移、Redis 预设答案缓存、后端编译、前端构建和热词直答 SSE 响应。
