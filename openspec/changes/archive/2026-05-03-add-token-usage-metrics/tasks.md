## 1. 数据库模型与迁移

- [x] 1.1 检查当前 `chat_message`、`chat_session`、`legal_user` 表结构和索引，确认按租户、日期、用户聚合 Token 的 SQL 口径。
- [x] 1.2 新增 `token_usage_metric_daily_summary` 表，包含租户、指标日期、统计版本、总 Token、消息数、活跃用户数、平均每消息 Token、Top 用户数量、状态、错误信息和时间字段；每个字段必须有数据库注释。
- [x] 1.3 新增 `token_usage_metric_top_user` 表，包含日汇总 ID、租户、指标日期、用户 ID、用户名、显示名称、排名、Token 消耗、消息数、占比、统计版本和时间字段；每个字段必须有数据库注释。
- [x] 1.4 为日汇总表增加 `tenant_id + metric_date + metric_version` 唯一约束，并为 Top 用户表增加日汇总、租户日期和用户维度查询索引。
- [x] 1.5 更新 `schema.sql` 和增量迁移 SQL，验证表注释、字段注释、唯一约束和索引可在 MySQL 正常执行。

## 2. 枚举、配置与基础类型

- [x] 2.1 新增 Token 指标配置类，配置前缀为 `token.metric.usage`，包含总开关、统计版本、Worker、Top 用户和重试相关配置；每个配置属性必须添加说明注释。
- [x] 2.2 更新 `application.yaml`，新增 Token 指标 YAML 配置，所有新增 YAML 条目必须添加中文注释。
- [x] 2.3 新增 Token 指标日汇总状态枚举，枚举类和每个枚举值必须添加说明注释。
- [x] 2.4 新增 Token 指标 Entity、Mapper 和 DTO；每个新增实体属性必须添加说明注释。
- [x] 2.5 在配置注册、Mapper 扫描和必要路由入口中接入 Token 指标模块。

## 3. 日汇总统计服务

- [x] 3.1 实现 Token 指标统计 SQL，按租户、指标日期聚合总 Token、消息数、活跃用户数和平均每消息 Token。
- [x] 3.2 实现 Top 用户聚合 SQL，按 Token 消耗降序、消息数降序、用户 ID 升序生成稳定排名，并按 YAML 配置数量截断。
- [x] 3.3 实现创建或复用日汇总逻辑，保证同一租户、指标日期和统计版本幂等。
- [x] 3.4 实现日汇总状态流转：处理中、完成、部分失败、失败，并记录错误信息和开始/完成时间。
- [x] 3.5 实现重复计算时删除并重建该日汇总下的 Top 用户明细。
- [x] 3.6 实现手动日期范围统计逻辑，只允许今天之前的历史日期，且已完成日期返回无需统计。
- [x] 3.7 实现定时任务统计逻辑，默认处理配置日期偏移对应的指标日期，并支持按批处理租户。

## 4. API 与权限

- [x] 4.1 新增管理员 Token 指标 Controller，复用当前管理员角色鉴权和租户上下文。
- [x] 4.2 实现日期范围 summary API，返回总 Token、消息数、活跃用户数、平均 Token、统计版本和趋势点。
- [x] 4.3 实现日汇总分页 API，支持按日期范围查询 Token 日汇总记录。
- [x] 4.4 实现 Top 用户明细 API，支持按日汇总 ID 查询每日 Token 消耗用户排行。
- [x] 4.5 实现手动运行 API，支持单日或历史日期范围统计，并返回选中、成功、失败、跳过和已处理日期数量。
- [x] 4.6 统一接口错误信息，确保当天或未来日期请求返回明确业务错误。

## 5. 定时任务与幂等保护

- [x] 5.1 新增 `TokenUsageMetricWorker`，使用 YAML Cron 触发低峰统计任务。
- [x] 5.2 使用本地防重入保护避免同实例任务重叠执行。
- [x] 5.3 在服务层通过唯一约束和状态更新保证重复触发不会创建重复日汇总。
- [x] 5.4 记录定时任务运行日志，包括日期、租户数、成功数、失败数、跳过数和错误摘要。

## 6. 前端看板

- [x] 6.1 新增 Token 指标前端 API 类型和请求方法，覆盖 summary、daily summaries、top users 和 run 接口。
- [x] 6.2 新增 Token 指标页面或管理入口，包含日期筛选、手动统计按钮和运行结果提示。
- [x] 6.3 使用 ECharts 展示每日 Token 消耗和消息数趋势。
- [x] 6.4 展示日期范围卡片指标：总 Token、总消息数、活跃用户数、平均每消息 Token。
- [x] 6.5 展示日汇总分页列表，点击某日汇总后加载该日 Top 用户排行。
- [x] 6.6 保持 `web/src/router/index.js` 与 `index.ts` 同步，避免开发环境解析到 JS 旧路由。

## 7. 测试与验证

- [x] 7.1 增加统计服务单元测试，覆盖空数据、正常聚合、多用户排名、Top 数量截断和稳定排序。
- [x] 7.2 增加幂等和日期范围测试，覆盖已完成日期跳过、失败日期重算、当天/未来日期拒绝。
- [x] 7.3 增加 API 定向测试或集成验证，覆盖 summary、分页、Top 用户和手动运行接口。
- [x] 7.4 运行后端编译和相关测试。
- [x] 7.5 运行前端构建或目标页面验证。
- [x] 7.6 使用 dbhub 或本地 SQL 验证新表、字段注释、索引和样例统计结果。

## 8. 发布与运维说明

- [x] 8.1 记录 Token 指标统计口径：统计来源、日期口径、消息范围、用户归属和零 Token 处理方式。
- [x] 8.2 记录 YAML 配置说明，包括任务开关、Cron、日期偏移、批大小、Top 用户数量和重试策略。
- [x] 8.3 记录灰度步骤：先关闭自动任务，手动统计单日验证，再开启低峰定时任务。
- [x] 8.4 记录回滚方案：关闭 `token.metric.usage.enabled`，保留新表，前端隐藏入口或停止访问接口。
