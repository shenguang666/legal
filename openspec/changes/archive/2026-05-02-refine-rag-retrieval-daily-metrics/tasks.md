## 1. 数据库模型与迁移

- [x] 1.1 检查当前 `retrieval_log` 和 `rag_retrieval_metric_evaluation` 表结构，并确认安全迁移 SQL。
- [x] 1.2 新增 `rag_retrieval_metric_daily_summary` 表，包含租户、指标日期、评估版本、计数器、平均召回率、平均精确率、状态、错误信息和时间字段；每个字段必须有数据库注释。
- [x] 1.3 为 `rag_retrieval_metric_evaluation` 增加 `daily_summary_id` 外键/索引字段，并兼容已有历史行。
- [x] 1.4 为 `retrieval_log` 增加 RAG 指标扫描状态、扫描时间、汇总引用、重试次数和错误信息字段；每个新增字段必须有数据库注释。
- [x] 1.5 增加或更新 schema 迁移文档，并验证租户/日期/版本/状态查询索引。

## 2. 枚举与配置

- [x] 2.1 创建检索日志扫描状态、日汇总状态和评估状态的 Java 枚举类；枚举类和枚举值必须包含说明注释。
- [x] 2.2 在服务、DTO 映射和测试中使用枚举替换 RAG 指标状态字符串字面量。
- [x] 2.3 更新 RAG 指标 YAML 配置，移除或废弃 `max-daily-samples`，增加低峰调度默认值，并保证每个 YAML 条目都有注释。
- [x] 2.4 增加指标日期偏移、批大小、重试策略、query 过滤、候选召回和 LLM 判断配置。

## 3. 日汇总批处理

- [x] 3.1 新增日汇总 Entity、Mapper 和 DTO，并为每个实体属性添加注释。
- [x] 3.2 重构指标 Worker，使其在低峰窗口每 5 分钟运行，并增量处理配置的指标日期。
- [x] 3.3 使用租户、指标日期和评估版本实现创建或锁定日汇总逻辑，保证幂等。
- [x] 3.4 扫描所有符合条件的 `retrieval_log` 行，不再设置每日最大样本上限；批大小仅作为单次运行处理限制。
- [x] 3.5 将每条已扫描检索日志更新为终态或可重试状态，并记录扫描时间、汇总 ID、重试次数和错误信息。
- [x] 3.6 持久化每条消息级评估记录及 `daily_summary_id`，并保留严格 LLM 失败处理、低质量 query 过滤、粗召回 + 精筛候选选择。
- [x] 3.7 每批处理后重新计算日汇总计数器和平均 Recall/Precision，并在没有待处理日志时标记汇总完成。

## 4. API 与聚合

- [x] 4.1 修改 summary API，使其按日期范围和租户从 `rag_retrieval_metric_daily_summary` 聚合。
- [x] 4.2 返回 RAG 消息总数、有效消息数、过滤数、成功数、失败数、跳过数、平均召回率、平均精确率和日趋势点。
- [x] 4.3 修改 detail API，使其支持按日汇总 ID 或日期范围分页查询子评估。
- [x] 4.4 增加指定指标日期的手动运行或重跑接口，并保证安全幂等。
- [x] 4.5 确保旧评估版本不会污染当前看板聚合。

## 5. 前端看板

- [x] 5.1 更新 RAG 指标 API 客户端类型，支持日汇总和子评估响应。
- [x] 5.2 使用日汇总 API 结果替代基于明细的趋势计算。
- [x] 5.3 增加 ECharts 趋势图，展示 Recall、Precision、RAG 消息数和有效消息数。
- [x] 5.4 增加日汇总表或日期选择交互，用于加载子评估明细。
- [x] 5.5 在看板清晰展示任务状态、有效/过滤/失败/跳过数量和评估版本。

## 6. 测试与验证

- [x] 6.1 增加枚举映射、指标日期选择、query 过滤和日汇总重新计算的单元测试。
- [x] 6.2 增加多批次处理、检索日志扫描状态流转、失败项重试和幂等复用日汇总的服务测试。
- [x] 6.3 增加日期范围 summary 和 detail 分页的 API 测试或定向集成检查。
- [x] 6.4 运行后端编译和定向指标测试。
- [x] 6.5 运行前端构建或目标页面验证。
- [x] 6.6 使用 dbhub 或本地 SQL 验证新表/字段、注释、索引和样例日汇总数据。

## 7. 发布文档

- [x] 7.1 记录新的日汇总指标口径、状态枚举、扫描生命周期和重试行为。
- [x] 7.2 记录 YAML 配置变更，包括移除/废弃 `max-daily-samples` 和低峰 Cron 默认值。
- [x] 7.3 记录新汇总表、子表外键和 `retrieval_log` 扫描字段的迁移与回滚步骤。
