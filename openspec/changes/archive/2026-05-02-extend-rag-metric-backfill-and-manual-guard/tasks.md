## 1. 扫描状态与日期选择

- [x] 1.1 梳理 `retrieval_log.rag_metric_scan_status` 当前取值和数据库字段注释，确认是否保留 `FILTERED` 或新增/迁移为 `SKIPPED`。
- [x] 1.2 新增服务方法判断指定业务日期是否存在待处理或可重试 RAG 日志，状态包含空、`PENDING`、`PROCESSING`、失败未超重试次数。
- [x] 1.3 新增服务方法查找默认归档日期之前最早的历史待处理业务日期，查询条件基于 `retrieval_log.created_at` 和 RAG 命中字段。
- [x] 1.4 确保所有新增实体属性、枚举值或 YAML 配置项都带中文注释；如未新增字段则更新相关注释说明扫描状态语义。

## 2. 定时任务历史补扫

- [x] 2.1 重构定时 Worker 调用入口，使其先处理配置日期，默认仍为上一自然日。
- [x] 2.2 在配置日期无待处理批次后，继续检查并处理更早历史待处理日期，每轮保持 `batch-size` 成本控制。
- [x] 2.3 确保历史补扫按消息所属业务日期获取或创建日汇总，并将消息级评估写入对应 `daily_summary_id`。
- [x] 2.4 每批处理后刷新受影响日汇总的总数、有效数、过滤/跳过数、成功数、失败数、平均 Recall 和平均 Precision。
- [x] 2.5 当某日期无剩余待处理或可重试日志时，正确标记日汇总为 `COMPLETED` 或 `PARTIAL_FAILED`。

## 3. 手动评估保护

- [x] 3.1 调整 `/api/rag-metrics/run` 手动评估接口参数，支持结束日期校验；如扩展日期范围，需兼容单日评估。
- [x] 3.2 在服务层限制手动评估结束日期必须早于服务器当前自然日，今天和未来日期返回明确错误。
- [x] 3.3 手动评估执行前检查目标日期是否已有当前评估版本完成日汇总且无待处理或可重试日志。
- [x] 3.4 对已完整处理的手动评估请求返回“已有记录，无需评估”的提示，不重复调用候选召回或 LLM 判断。
- [x] 3.5 扩展 `RunResult` 或新增响应 DTO，返回实际处理日期、选中数量、成功/失败/跳过数量和跳过原因，并为新增字段添加注释。

## 4. 前端与交互提示

- [x] 4.1 调整 RAG 指标页面的手动评估日期选择，禁止选择今天及未来日期作为结束日期。
- [x] 4.2 手动评估接口返回已处理提示时，在页面展示“已有记录，无需评估”或等价提示。
- [x] 4.3 手动评估完成后刷新日汇总分页列表、趋势图和当前选中日期范围统计。

## 5. 测试与验证

- [x] 5.1 增加服务测试：默认日期处理完后能选择更早历史待处理日期补扫。
- [x] 5.2 增加服务测试：历史失败日志未超过最大重试次数时可重试，超过后不再自动处理。
- [x] 5.3 增加服务测试：补扫或重试后对应日期日汇总平均 Recall、平均 Precision 和计数会重新计算。
- [x] 5.4 增加接口测试：手动评估结束日期为今天或未来日期时被拒绝。
- [x] 5.5 增加接口测试：手动评估已完整处理日期时返回无需重复评估提示。
- [x] 5.6 运行后端编译和 RAG 指标相关测试，运行前端构建或目标页面验证。
