## 1. 配置与数据模型

- [x] 1.1 新增文档处理配置属性类，覆盖默认解析方式、最大上传文档数量、MinerU 启用开关、基础地址、Token、模型版本、语言、表格/公式识别、轮询间隔、超时时间、重试次数和 worker 并发。
- [x] 1.2 在 `application.yaml` 增加文档处理和 MinerU 配置项，并为每一条新增 YAML 配置添加中文注释。
- [x] 1.3 设计并落库解析状态数据结构，扩展 `kb_document` 或新增解析任务表，保存解析方式、解析状态、MinerU batch ID、data ID、结果 URL、失败原因、重试次数、开始/完成时间。
- [x] 1.4 新增或更新实体类、枚举和 Mapper，确保每个新增实体属性和每个枚举类/枚举值都有中文注释。

## 2. 解析器抽象与 MinerU 客户端

- [x] 2.1 新增统一文档解析接口和解析结果模型，支持原始文本、Markdown、结构块、解析方式、外部任务标识和错误信息。
- [x] 2.2 将现有 Tika 抽取逻辑封装为原生解析器，保证未选择 MinerU 时行为与现有导入链路兼容。
- [x] 2.3 实现 MinerU v4 客户端，支持申请批量上传 URL、PUT 上传文件、轮询批次结果、下载结果 zip 并读取 `full.md`。
- [x] 2.4 实现 MinerU 错误映射和安全日志策略，确保 Token 不进入前端响应、业务错误信息或普通日志。
- [x] 2.5 实现 MinerU 解析任务 worker，处理 pending、processing、completed、failed 状态流转和可重试失败。

## 3. 语义切片与导入链路改造

- [x] 3.1 实现结构优先的语义切片器，按标题、段落、条款、列表和表格块组织 chunk，并保留最大长度限制。
- [x] 3.2 为超长结构块增加句子/行边界拆分和最终安全字符窗口兜底，避免生成超大 chunk。
- [x] 3.3 改造知识库文档导入链路，支持解析方式参数、上传数量校验、异步解析状态和解析完成后投递 ES 索引。
- [x] 3.4 改造风险规则文件导入链路，支持解析方式参数，并保证规则定义与文档解析/索引状态一致。
- [x] 3.5 改造天眼审查文档导入链路，确保解析未完成或失败的文档不能直接进入审查流程。
- [x] 3.6 增加解析重试接口或复用现有文档操作入口，允许管理员对可重试失败任务重新发起解析。

## 4. 前端上传体验

- [x] 4.1 新增后端能力接口或扩展现有接口，向前端返回可用解析方式、默认解析方式和最大上传文档数量。
- [x] 4.2 改造 `KnowledgeView.vue`，增加解析方式选择、最大上传数量提示、必要的多文件选择和解析状态/失败展示。
- [x] 4.3 改造 `TianyanView.vue`，增加解析方式选择，并在文档解析完成前禁用或提示审查入口。
- [x] 4.4 改造 `RiskRuleView.vue`，增加规则文件导入的解析方式选择和解析失败提示。
- [x] 4.5 确认 `web/src/router/index.js` 与 `index.ts` 的运行期同步要求，避免前端路由或导入解析受 JS 同名文件影响。

## 5. 验证与发布准备

- [x] 5.1 增加后端单元测试，覆盖上传数量超限、默认解析方式、原生解析兼容、MinerU 成功解析和 MinerU 失败状态。
- [x] 5.2 增加语义切片测试，覆盖标题段落、编号条款、表格、超长段落和纯文本兜底切片。
- [x] 5.3 增加天眼审查前置校验测试，确认解析未完成或失败时不会启动合同审查。
- [x] 5.4 使用 `mvn -DskipTests compile` 和相关后端测试验证编译与核心逻辑。
- [x] 5.5 使用 `npm run build` 验证前端解析方式选择和上传页面构建通过。
- [x] 5.6 准备 MinerU Token 未配置、MinerU 关闭和 MinerU 开启三种部署说明与回滚步骤。
